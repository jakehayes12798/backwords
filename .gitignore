# .NET
bin/
obj/
*.user
*.suo

# Visual Studio
.vs/
*.sln.docstates

# SQLite (generated/local databases - the schema/seed scripts are what's tracked, not the db file itself)
*.db
*.sqlite
*.sqlite3

# Node (client/ has its own .gitignore too, but this covers the root just in case)
node_modules/
dist/
dist-ssr/
*.local

# Editor / OS noise
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
